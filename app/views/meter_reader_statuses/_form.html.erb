<%= form_for(@meter_reader_status) do |f| %>
  <% if @meter_reader_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meter_reader_status.errors.count, "error") %> prohibited this meter_reader_status from being saved:</h2>

      <ul>
      <% @meter_reader_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
