<%= form_for(@meter_status) do |f| %>
  <% if @meter_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meter_status.errors.count, "error") %> prohibited this meter_status from being saved:</h2>

      <ul>
      <% @meter_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :billing_meter_status %><br>
    <%= f.text_field :billing_meter_status %>
  </div>
  <br/>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
