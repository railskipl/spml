  <div class="row">
        
    <div class="span12">          
          
          <div class="widget stacked ">
            
            <div class="widget-header">
              <i class="icon-pencil"></i>
              <h3>Listing Mobile Users</h3>
          </div> <!-- /widget-header -->
        
             <div class="widget-content">


<table cellpadding="0" cellspacing="0" border="0" class="table table-striped table-bordered table-highlight">
<thead>
  <tr>
    <th>Sr. No.</th>
    <th>Imei</th>
    <th>Name</th>
    <th colspan=2>Action</th>
  </tr>
</thead>
<% @mobileusers.each.with_index(1) do |mobileuser,index| %>
 <tr>
  <td><%= index %></td>
	<td><%= mobileuser.imei%></td>
   <td> <%= mobileuser.user.first_name rescue nil %></td>

  <td> 
   <%= link_to 'Edit', edit_mobileuser_path(mobileuser) ,:class=>"btn"%>
 </td>
  <td><a href="/mobileusers/<%= mobileuser.id %>/delete" onclick="if(confirm('Are you sure you want to delete this page?'))  return true;  else  return false;" class="btn" >Delete</a></td>
   </tr>   
<% end %>
</table>
<p><%= link_to "New Mobile User", new_mobileuser_path,:class =>"btn btn-secondary" %></p>
</div>  
</div>
</div>
</div>